services:
  docs:
    image: pjmeca/spotcrate-docs
    container_name: spotcrate-docs
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    ports:
      - "80:80"
    volumes:
      - ./docs:/app/docs
      - ./mkdocs.yml:/app/mkdocs.yml
